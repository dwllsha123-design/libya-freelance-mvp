node_modules/
dist/
.next/
.env
.env.local
.env.*.local
.env.production
.env.staging
backend/.env
backend/.env.production
backend/.env.staging
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.staging
*.log
.DS_Store
coverage/
.turbo/
backend/.prod-verify/
backend/.runtime-bundle/
backend/data/

# TypeScript incremental build cache (regenerated by every build)
*.tsbuildinfo

deploy-logs/
*.pem
*.key
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
known_hosts.local

# Local VAPID material — never commit
**/.vapid.keys.local
**/*.vapid.local
backend/.vapid.keys.local

# Reverse proxy — managed on server, not by app deploy
Caddyfile
Caddyfile.*
caddy/
.caddy/
